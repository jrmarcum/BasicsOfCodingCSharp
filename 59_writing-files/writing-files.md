Requires a `tmp/` directory to exist at runtime. Create it manually before running.

___

##### Run Command:
`dotnet run writing-files.cs`

##### Results:
`wrote 5 bytes`
`wrote 7 bytes`
`wrote 9 bytes`
